# Generated by `agentbox apply` - do not edit.
abi <abi/4.0>,
include <tunables/global>

profile agent-box-rootlesskit /nix/store/*/bin/rootlesskit flags=(unconfined) {
  userns,

  # Site-specific additions and overrides, the same hook Ubuntu's own
  # profiles carry. Never written by us, so an operator's additions
  # survive every apply.
  include if exists <local/agent-box-rootlesskit>
}

profile agent-box-slirp4netns /nix/store/*/bin/slirp4netns flags=(unconfined) {
  userns,

  # Site-specific additions and overrides, the same hook Ubuntu's own
  # profiles carry. Never written by us, so an operator's additions
  # survive every apply.
  include if exists <local/agent-box-slirp4netns>
}
