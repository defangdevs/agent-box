# Generated by `agentbox apply` - do not edit.
[Unit]
Description=agent-box: early OOM daemon
After=agent-box-zram.service

[Service]
Type=simple
ExecStart=@PROFILE@/bin/earlyoom -m 10 -s 10 -r 3600 --avoid '^(sshd|systemd|systemd-.*|caddy|ttyd|tmux.*|amazon-ssm-agen|ssm-.*|nix-daemon)$' --prefer '^(node|claude|codex)$'
Restart=always
RestartSec=5s
OOMScoreAdjust=-100

[Install]
WantedBy=multi-user.target
